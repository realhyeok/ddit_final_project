<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Alert-Mapper">

	<insert id="insertAlert" parameterType="Map">
		INSERT INTO ALERT (
			USER_ID
			,NICKNAME
			,WRITE_TIME
			,CONTENT) 
		VALUES (
			#{receiverUserId}
			, #{serderNickName}
			, SYSDATE
			, #{content}
		)
	</insert>
	
	<select id="getUserAlertList" parameterType="String" resultType="Alert">
		SELECT
			USER_ID
			,NICKNAME
			,WRITE_TIME
			,CONTENT
		FROM ALERT
		WHERE USER_ID = #{userId}
	</select>
	
</mapper>